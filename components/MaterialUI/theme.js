import { createTheme } from "@mui/material";
export const colors = {
    primary: '#56af46',
    primaryLigth: '#c8ffb1',
}
export const theme = createTheme({
    palette: {
        primary: {
            main: '#56af46',
        },

    },
});

